# Copy to .env.local for local development. Never commit real values.

# Neon pooled connection string (dev branch locally, main branch in Vercel)
DATABASE_URL=

# 32+ random bytes, e.g. `openssl rand -base64 48`
SESSION_SECRET=

# Protects /api/cron (used from M3 onward)
CRON_SECRET=
